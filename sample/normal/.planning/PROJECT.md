# Sample Project — Robot Coffee Service

A demo GSD workspace for manually testing gsd-status. A fictional project
about a robot that fetches coffee, with enough phases, steps, and documents
to exercise the status panel, step navigation, and every document tab kind.

## Goals

- One verified phase, one executing phase (the "current" phase), one untouched.
- The executing phase has three steps and all phase-level documents.
