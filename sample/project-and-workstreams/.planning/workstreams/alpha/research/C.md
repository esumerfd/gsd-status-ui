# Research C: Reservation UX Patterns

Alpha's own `research/` directory. Once this exists, `gsd-status`'s folder
federation shows only this file for the Research section while alpha is
focused — the root's `A.md` and `B.md` become invisible, not merged in.
