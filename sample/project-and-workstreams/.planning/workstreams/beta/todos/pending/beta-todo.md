---
title: Retry failed notification sends
---

# Retry failed notification sends

Lives under `workstreams/beta/todos/pending/` — scoped to beta, and per
`OWNED_FOLDERS`/`load_todos`, invisible in `gsd-status`'s rendered output no
matter which workstream is focused.
